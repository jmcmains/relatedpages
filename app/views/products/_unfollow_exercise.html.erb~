<%= form_for(exercise.related_exercises.find_by(product_id: product.id),
             html: { method: :delete, class: "form-inline", style: "display:inline" }, remote: true) do |f| %>
  <%= f.submit "- #{exercise.name}", class: "btn btn-success btn1" %>
<% end %>
